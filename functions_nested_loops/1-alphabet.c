#include "main.h"
/**
 * main - Print the alphabet  in upercase.
 *
 * Return: Send when the programe succes
 */
int main(void)
{
	char n;

	for (n = 'a'; n <= 'z'; n++)
	{
		_putchar(n);
	}
	_putchar('\n');
	return (0);
}
