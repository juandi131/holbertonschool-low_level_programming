#include "main.h"

/**
 *  _isdigit- me escape del mundo yedome al norte
 *
 * Return: La haces muy bien
 */

int _isdigit(int c)
{

	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else

		return (0);
}
