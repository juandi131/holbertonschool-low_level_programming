#include "main.h"

/**
 *_isupper- Me escape del mundo yedome al norte. 10
 *@c: variable
 *Return: La haces muy bien
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
		return (0);
}
